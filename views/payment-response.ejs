<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Response</title>
    <!-- Include any required CSS/JS files here -->
</head>
<body>
    <div class="container">
        <h1 class="text-center text-3xl font-bold mb-6">Payment Response</h1>
        <div class="max-w-3xl mx-auto bg-white rounded-lg shadow-md p-6">
            <% if (errors && errors.length > 0) { %>
                <h2 class="text-red-600 text-xl mb-4">Validation Errors</h2>
                <ul>
                    <% errors.forEach(error => { %>
                        <li><%= error.msg %></li>
                    <% }); %>
                </ul>
            <% } else if (error) { %>
                <h2 class="text-red-600 text-xl mb-4">Error</h2>
                <p><%= error %></p>
            <% } else if (cardInfo) { %>
                <h2 class="text-green-600 text-xl mb-4">Payment Successful!</h2>
                <p>Card Info: <%= JSON.stringify(cardInfo, null, 2) %></p>
            <% } %>
        </div>
    </div>
</body>
</html>